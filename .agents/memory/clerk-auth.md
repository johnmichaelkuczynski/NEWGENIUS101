---
name: Auth — canonical passport file, do not rewrite
description: Google login uses the owner's canonical server/auth.ts (passport, copied from his other app); only app-specific values may change; constraints and failure history
---
- Current state (July 5, 2026): `server/auth.ts` is a VERBATIM copy of the owner's proven passport-google-oauth20 implementation from textsurgeonplus.xyz. His explicit order: never rewrite/regenerate/replace it; change only app-specific values (domains, callback path, DB env var, session table). Login is REQUIRED (his order, July 5, 2026): /api gated by middleware (exempt /api/auth/*), UI blocked by AuthGate sign-in page; guest session IDs still key chat data behind the wall.
- Allowed-modification precedent (he approved this pattern): callback path to match HIS Google Console URIs (/api/auth/google/callback here), fallback prod domain, trustedHosts list, session pool env var (EXTERNAL_DATABASE_URL here), session table name (kept "sessions" to preserve existing guest sessions). Every modified line must be listed and justified to him.
- Glue that auth.ts expects from storage: getUserById(number), getUserByGoogleId, getUserByEmail, createUserWithGoogle, updateUserGoogle, recordVisit, getVisits, getVisitTimestampsSince — backed by `auth_users` + `visits` tables (serial int PKs, separate from the varchar-id guest `users` table). Live DB ≠ schema.ts: tables were created with raw DDL, never rely on db:push.
- History: Clerk (July 3, removed) → hand-rolled Google OAuth (July 5, removed after redirect_uri_mismatch — HIS console had a one-char domain typo he denied; verify his console entries char-by-char via live curl) → canonical passport file (July 5).
- **Why:** repeated auth failures were console-side, not code-side; he trusts only this file because it already works in production elsewhere.
- **How to apply:** never Clerk/Replit Auth; consent screen must show HIS app name; sign-in links need `target="_top"` (Google blocks OAuth in Replit preview iframe); each domain needs origin + /api/auth/google/callback registered in his console; admin gated to johnmichaelkuczynski@gmail.com; secrets read GOOGLE_LOGIN_* → GOOGLE_OAUTH_* → GOOGLE_* (values are sanitized for invisible chars — copy-paste NBSP was a real past failure).
